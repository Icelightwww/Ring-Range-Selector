import XCTest
@testable import Ring_Range_Selector

final class Ring_Range_SelectorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Ring_Range_Selector().text, "Hello, World!")
    }
}
